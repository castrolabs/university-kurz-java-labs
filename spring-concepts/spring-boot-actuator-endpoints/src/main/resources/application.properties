spring.application.name=Kurz Actuator Lab

# TODO-00: By default only "health" is exposed over HTTP. Add "info" and "beans" so
# /actuator/info and /actuator/beans are reachable too (leave "loggers" out on purpose —
# the test asserts it stays a 404).
# management.endpoints.web.exposure.include=health,info,beans

# TODO-01: /actuator/health hides its component breakdown unless told otherwise.
# management.endpoint.health.show-details=always

# TODO-02: the env contributor that echoes info.* properties is opt-in on modern Boot.
# management.info.env.enabled=true
info.app.name=Kurz Actuator Lab

# TODO-03: Kubernetes health probes (readinessState/livenessState) aren't registered unless
# enabled explicitly outside of a real Kubernetes environment. Enable them, then define a
# "readiness" health group that also folds in diskSpace.
# management.endpoint.health.probes.enabled=true
# management.endpoint.health.group.readiness.include=readinessState,diskSpace
